{
  "component": {
    "divider": {
      "$description": "Structural tokens for Divider component.",
      "thickness": {
        "$type": "dimension",
        "$value": "{semantic.border.width.default}",
        "$description": "1px — thin rule."
      },
      "spacing": {
        "$type": "dimension",
        "$value": "{semantic.space.gap.lg}",
        "$description": "16px — default margin around divider."
      }
    }
  }
}
