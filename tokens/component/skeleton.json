{
  "component": {
    "skeleton": {
      "$description": "Dimensões de placeholders do Skeleton.",
      "text-height":  { "$type": "dimension", "$value": "{foundation.spacing.4}",  "$description": "16px — altura de 1 linha de texto simulada" },
      "circle-size":  { "$type": "dimension", "$value": "{foundation.spacing.10}", "$description": "40px — avatar circular simulado" },
      "rect-height":  { "$type": "dimension", "$value": "7.5rem",                   "$description": "120px — placeholder de bloco (card, imagem). Valor fora da escala foundation." }
    }
  }
}
